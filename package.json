{
  "name": "weekly-meal-planner",
  "version": "1.0.0",
  "description": "Un mic web-tool static care te ajută să îți planifici mesele pentru o săptămână și să generezi automat lista de cumpărături, totul din browser – fără backend și fără instalare!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sitemap": "node scripts/generate-sitemap.cjs",
    "build:js": "esbuild public/js/app.js --bundle --minify --external:./recipes-budget.js --outfile=public/js/app.min.js && esbuild public/js/checkout.js --bundle --minify --outfile=public/js/checkout.min.js && esbuild public/js/portal.js --bundle --minify --outfile=public/js/portal.min.js",
    "build:css": "esbuild public/css/style.css --minify --outfile=public/css/style.min.css",
    "content": "node scripts/generate-content.mjs",
    "build": "npm run build:js && npm run build:css && npm run content"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BucosAlexandru/weekly-meal-planner.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "bugs": {
    "url": "https://github.com/BucosAlexandru/weekly-meal-planner/issues"
  },
  "homepage": "https://github.com/BucosAlexandru/weekly-meal-planner#readme",
  "dependencies": {
    "@react-pdf/renderer": "^4.5.1",
    "@supabase/supabase-js": "^2.52.0",
    "dotenv": "^17.2.2",
    "micro": "^10.0.1",
    "nodemailer": "^7.0.5",
    "openai": "^5.20.3",
    "stripe": "^18.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "sharp": "^0.34.5",
    "vercel": "^48.10.2"
  }
}
